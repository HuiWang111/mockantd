export enum ButtonSize {
    Default = 'default',
    Large = 'large',
    Small = 'small'
}
  
export enum ButtonType {
    Default = 'default',
    Primary = 'primary',
    Danger = 'danger',
    Link = 'link',
    Dashed = 'dashed'
}
  
export enum ButtonShape {
    Default = 'default',
    Circle = 'circle',
    Round = 'round'
}